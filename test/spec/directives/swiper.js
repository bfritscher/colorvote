'use strict';

describe('Directive: swiper', function () {

  // load the directive's module
  beforeEach(module('colorvoteApp'));

  var element,
    scope;

  beforeEach(inject(function ($rootScope) {
    scope = $rootScope.$new();
  }));
});
